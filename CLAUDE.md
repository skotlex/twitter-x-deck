# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 자동으로 읽는 컨텍스트다.
작업 전 전체를 읽고, "규칙" 섹션을 위반하지 않는다.

## 1. 커밋 정책

- 작업 브랜치는 `main`. 작업 전 현재 브랜치를 확인하고, 다른 브랜치면 임의로 전환하지 말고 사용자에게 알린다
- 소스 수정이 포함된 작업을 완료하면 사용자에게 묻지 않고 **commit 까지 수행**한다.
  **push 는 절대 하지 않는다** (사용자가 직접 push). 원격 브랜치 생성·MR 도 마찬가지
- **커밋 메시지는 반드시 한글로 작성** (예외 없음, 파일 경로·식별자는 영어 그대로 가능).
  변경 결과를 요약하고, CHANGELOG 에 담지 못한 기술 세부는 여기에 적는다
- 이번 작업에 해당하는 파일만 stage 한다. 작업과 무관한 기존 변경이나 untracked 파일을
  끌어들이지 않는다. 함께 수정한 문서·이번 작업에서 만든 신규 파일은 누락 없이 같은 커밋에 포함
- `CHANGELOG.md`·계획 문서 등 **문서만 수정한 작업은 commit 하지 않는다**
- 버그 수정 commit 후 사용자가 "수정 안 됐다"고 하면: **push 전 한정**으로 직전 커밋이면
  `git commit --amend`, 그 이전이면 `git reset --soft` 후 재커밋으로 갈아끼운다 —
  실패한 수정 커밋을 히스토리에 남기지 않는다. 커밋 여러 개의 이력을 다시 써야 하면 먼저 사용자 확인
- 이미 push 된 커밋(ahead 카운트로 판단)은 재작성 금지 — 후속 수정 커밋으로 처리
- 커밋 직전 `git status` 로 stage 목록을 확인한다. PAT·토큰·메일 데이터가 든 파일이
  올라가지 않는지 본다 (규칙 4.1)

---

## 2. CHANGELOG 자동 작성

수정 요청을 처리한 뒤, **사용자가 체감하는 변경이면 [CHANGELOG.md](CHANGELOG.md) 에
자동 반영**한다 (요청자가 따로 말하지 않아도 수행).

- **포함**: 새로운 기능 · 편의성 개선 · 버그 수정 — 단 **동작이 검증된 것만**.
  셀렉터 추측 반영처럼 아직 확인되지 않은 변경은 적지 않는다
- **제외**: 버튼 위치·색상 등 단순 UI 변경, 내부 리팩토링·파일 구조 변경 등 사용자가
  체감하지 않는 변경
- **분류**: `### 새로운 기능` / `### 편의성 개선` / `### 버그 수정` 중 맞는 섹션에
  한글 항목(`- 내용`)으로 작성
- **후속 수정·회귀 수정**, 현재 릴리스 항목에 이미 포함된 기능의 refinement 는 새 항목을
  만들지 말고 기존 항목 보강 또는 생략을 우선한다. 별개의 체감 변경만 새 항목으로
- **분량**: 한 항목은 **사용자 체감 현상만 1줄**. 동작 원리·DOM 셀렉터·API 호출 방식·
  "원인 → 해결" 설명을 적지 마라. `—` 뒤 부연이나 괄호 안 보충설명으로 기술 세부가 새어
  들어가지 않게 한다. 구현 직후라 코드 세부가 머릿속에 있어도 **결과적으로 무엇이 바뀌는지**만 남긴다
- **분량 상한(기계적 체크, 예외 없음)**: 한 항목 **공백 포함 80자 이내**. 항목을 추가·수정한
  턴의 마지막에 **반드시 최상단 블록의 각 항목 길이를 확인**하고, 초과 항목은 기능명만 남기고
  그 자리에서 줄인 뒤 작업을 끝낸다. "이 세부는 아깝다"고 느껴지는 정보일수록 빼는 게 맞다 —
  세부가 필요하면 커밋 메시지에 적는다

  ```powershell
  Get-Content CHANGELOG.md -TotalCount 40 | Where-Object { $_ -match '^-' -and $_.Length -gt 80 }
  ```

- **문체**: 결과 중심 간결 서술. `~합니다`·`~처리됩니다` 같은 문장형 종결어미 금지
- **버전 블록**: 최상단 버전 블록의 날짜가 오늘과 다르면 `## [버전] - YYYY-MM-DD` 블록을
  최상단에 신규 생성 (버전 = 직전 patch +1, 예: 0.1.2 → 0.1.3). 같은 날짜 블록이 이미 있으면
  그 블록에 항목만 추가. 첫 릴리스 전까지는 `## [Unreleased]` 에 쌓는다
- **버전 동기화**: 버전의 단일 출처는 `package.json` 의 `version` 이다. 새 버전 블록을 만들면
  `package.json` 을 갱신해 같은 커밋에 포함한다. `dist/manifest.json` 의 `version` 은
  `scripts/build.mjs` 가 빌드 시 여기서 주입하므로 저장소의 `manifest.json` 값은 손대지 않는다
- minor/major 를 올리는 큰 릴리스는 사용자에게 확인
- Keep a Changelog 형식 엄수